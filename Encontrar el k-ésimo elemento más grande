#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main(){
    vector<int> v = {7,10,4,3,20,15};
    int k = 3;
    sort(v.begin(), v.end(), greater<int>());
    cout << k << "-ésimo elemento más grande: " << v[k-1] << endl;
    return 0;
}
