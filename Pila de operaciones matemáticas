#include <iostream>
#include <stack>
#include <string>
using namespace std;

int main() {
    stack<string> operaciones;
    operaciones.push("5 + 2");
    operaciones.push("3 * 4");
    operaciones.push("10 / 2");

    cout << "Última operación: " << operaciones.top() << endl;
    operaciones.pop();

    cout << "Operaciones restantes:\n";
    while (!operaciones.empty()) {
        cout << operaciones.top() << endl;
        operaciones.pop();
    }
    return 0;
}
