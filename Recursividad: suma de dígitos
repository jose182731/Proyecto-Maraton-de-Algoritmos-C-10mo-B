#include <iostream>
using namespace std;

int sumaDigitos(int n) {
    if (n == 0) return 0;
    return (n % 10) + sumaDigitos(n / 10);
}

int main() {
    int n;
    cout << "Ingrese un número: ";
    cin >> n;
    cout << "Suma de sus dígitos: " << sumaDigitos(n) << endl;
    return 0;
}
