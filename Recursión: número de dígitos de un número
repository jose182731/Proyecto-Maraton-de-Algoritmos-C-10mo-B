#include <iostream>
using namespace std;

int contarDigitos(int n) {
    if (n == 0) return 0;
    return 1 + contarDigitos(n / 10);
}

int main() {
    int n;
    cout << "Ingrese un número: ";
    cin >> n;
    cout << "Número de dígitos: " << contarDigitos(n) << endl;
    return 0;
}
