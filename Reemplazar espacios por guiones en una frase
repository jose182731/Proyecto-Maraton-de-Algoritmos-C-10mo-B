#include <iostream>
#include <string>
using namespace std;

int main() {
    string frase;
    cout << "Ingrese una frase: ";
    getline(cin, frase);

    for (char &c : frase) {
        if (c == ' ') c = '-';
    }

    cout << "Frase modificada: " << frase << endl;
    return 0;
}
