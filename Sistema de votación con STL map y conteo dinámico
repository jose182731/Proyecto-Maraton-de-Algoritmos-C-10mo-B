#include <iostream>
#include <map>
#include <string>
using namespace std;

int main(){
    map<string,int> votos;
    votos["Ana"]++; votos["Luis"]++; votos["Ana"]++; votos["Marta"]++;

    for(auto &v:votos)
        cout<<v.first<<": "<<v.second<<" votos\n";
    return 0;
}
